{
  "name": "syncboard",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "turbo": "^2.10.8"
  },
  "devEngines": {
    "packageManager": {
      "name": "npm",
      "version": "12.0.1"
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
